# Cryptocurrency Outlook — プロジェクト仕様書

## 概要

仮想通貨トレーディングシグナル分析ツール。マルチタイムフレーム技術分析、デリバティブ分析、ニュース・センチメントを統合し、エントリーシグナルを生成する。

* **フレームワーク**: Next.js 14.2 (App Router) + React 18 + TypeScript 5
* **チャート**: lightweight-charts v5 (TradingView)
* **スタイル**: Tailwind CSS (ダークテーマ + 6テーマ切替)
* **デプロイ**: Vercel (東京リージョン hnd1)
* **言語**: UIは日本語

## コマンド

```
npm run dev      # 開発サーバー (localhost:3000)
npm run build    # プロダクションビルド
npm run lint     # ESLint
npx tsc --noEmit # 型チェック
```

## アーキテクチャ

```
src/
├── app/
│   ├── page.tsx              # メインページ
│   └── api/                  # APIルート (11エンドポイント)
├── components/               # UIコンポーネント (20ファイル)
├── hooks/                    # カスタムフック (2ファイル)
└── lib/                      # 分析モジュール (22ファイル)
```

### データフロー

```
ユーザー入力 (シンボル + タイムフレーム + トレードスタイル)
  → /api/analyze (メインオーケストレータ)
  → 並列データ取得 (Hyperliquid / Binance)
  → generateSignal() (signal.ts)
  → AnalysisResult JSON → クライアント描画
```

## 実装済み機能一覧

### データソース

| ソース | 認証 | 用途 |
| --- | --- | --- |
| Hyperliquid API | 不要 | ローソク足、OI、資金調達率、板情報、約定履歴 |
| Binance Futures API | 不要 | ローソク足(Taker買い出来高付き)、OI履歴、Top Trader比率 |
| Alternative.me | 不要 | Fear & Greed Index |
| Forex Factory | 不要 | 経済カレンダー |
| RSS Feeds (CoinTelegraph等) | 不要 | ニュース取得 |

### テクニカル指標 (`indicators.ts`)

RSI(14), MACD(12/26/9), SMA(20/50/200), EMA(20/50), VWAP, ボリンジャーバンド(20,2σ), ADX(14), StochasticRSI(14/14/3/3), ATR(14)

### 分析モジュール

| モジュール | ファイル | 概要 |
| --- | --- | --- |
| トレンド分析 | `trend.ts` | HH/HL検出、MA配列、トレンド強度、レンジ幅ベースのトレンド補助判定 |
| パターン検出 | `patterns.ts` | ローソク足パターン、チャートフォーメーション、偽ブレイクアウト、ウィック拒否 |
| サポレジ | `support-resistance.ts` | ピボットベース検出、クラスタリング、強度ランク、心理的節目ボーナス |
| デリバティブ | `derivatives.ts` | OI×価格シグナル、資金調達率過熱、プレミアム指数、OI残存率分析 |
| ボリュームプロファイル | `volume-profile.ts` | 30ビンVRVP、POC/VAH/VAL、買い/売りデルタ推定 |
| 清算レベル | `liquidation.ts` | 5x〜100xレバレッジの清算ゾーン、マグネット検出 |
| オーダーフロー | `order-flow.ts` | Taker買い/売り比率、インバランススコア |
| ダイバージェンス | `divergence.ts` | RSI/MACDダイバージェンス (通常/ヒドゥン) |
| マーケットレジーム | `market-regime.ts` | trending\_up/down, ranging, volatile, quiet |
| ホエール検出 | `whale-detection.ts` | 大口取引、板の壁、集積/分配シグナル |
| 群集心理 | `crowd-psychology.ts` | ショートスクイーズリスク、ロングスクイーズリスク、FOMO買い、パニック売り検出 |
| ニュース | `news.ts` | ニュース集約、関連度スコアリング(0-10)、リスク方向タグ |
| センチメント | `sentiment.ts` | Fear & Greed、逆張りシグナル |
| 経済カレンダー | `economic-calendar.ts` | 高インパクトイベント予測、信頼度調整 |

### トレードスタイル対応

3つのトレードスタイルをサポートし、スタイルに応じてタイムフレームの組み合わせ・重み付け・役割が切り替わる。

| スタイル | 環境認識 | セットアップ | トリガー | 執行 | 保有目安 |
| --- | --- | --- | --- | --- | --- |
| スキャルピング | 1h | 15m | 5m | 5m | 〜1時間 |
| デイトレード | 4h | 1h | 15m | 5m | 〜8時間 |
| スイング（デフォルト） | 1d | 4h | 1h | 15m | 〜3日 |

各スタイルは `TradingStyleConfig` で定義され、以下を含む:
* `timeframes`: 使用するタイムフレーム一覧
* `weights`: 各タイムフレームの重み付け（使用しない足は0）
* `roles`: 環境認識 / セットアップ / トリガー / 執行の役割割当
* `maxHoldingMs`: 最大保有時間

UIのスタイル選択タブで切り替え可能。デフォルトは「スイング」（後方互換）。

### シグナル生成 (`signal.ts` — 中核モジュール)

* **結論タイプ**: `enter_long` / `enter_short` / `wait` / `skip`
* **エントリー**: 指値注文 (S/Rベース、現在値から十分に引きつけた強いレベルを選択)
* **SL**: 構造ベース (直近S/Rレベル) + ATRガードレール (0.5×〜2.0×ATR)。絶対最低幅0.7%。S/Rが見つからない場合は0.7×ATRをフォールバックに使用。
* **blendedAtr**: セットアップ足のATR 70% + トリガー足のATR 30% のブレンド
* **タイムフレーム重み付け**: トレードスタイルに応じて動的に変化（`TradingStyleConfig.weights`）
* **信頼度スコア**: 0-100 (weak/moderate/strong)、不足データの明示付き
* **階層分析**: 環境認識足バイアス → セットアップ足波動 → トリガー足エントリータイミング
* **階層フィルター**: 環境認識足のバイアスに逆行するエントリーを段階的に抑制（信頼度減衰方式）。カウンタートレンドでもPR2.5以上で許容（警告付き）
* **価格丸め**: 最終出力で価格帯に応じた実用的な単位に丸め（BTC=$10単位、ETH=$5単位等）、ユーザーに有利な方向に丸める
* **環境認識バイアス乗数**: シグナル集計で環境認識足のトレンド方向と逆行するシグナルを×0.5減衰、順行を×1.3ブースト
* **実験的調整**: ニュース感情 / ホエール活動 / 群集心理による結論修正

### エントリー選定ロジック

指値エントリーは「最も近いS/R」ではなく「十分に引きつけた上で最も強いS/R」を選択する:
* 最低距離: ATR × 0.3 以上離れたレベルから探索
* 最大距離: ATR × 2.0 まで探索
* 探索範囲内で `strength` が最も高いレベルを優先
* 範囲内に該当なければ、最低距離以上で最も近いレベルにフォールバック

### S/Rレベル統合 (`mergeLevels`)

各タイムフレームのS/Rを統合する際、以下を実施:
* タイムフレーム重みによる強度ブースト
* VRVP レベル (POC/VAH/VAL) の注入
* 0.3%以内のレベルをクラスタリング
* **心理的節目ボーナス**: $1,000/$5,000/$10,000単位の丸い数字に近いレベルの強度を加算

### トレンド分析 (`trend.ts`)

* HH/HL（高値切り上げ/安値切り上げ）検出（最小振幅フィルタ付き: 4H=2%, 1d=3%）
* MA配列（SMA20/50/200の並び）
* ADXによる強度判定
* **レンジ幅ベースのトレンド補助判定**: タイムフレーム独立のルックバック期間（`RANGE_LOOKBACK`: 4H=80本/約13日）で、レンジ幅が5%以上の場合にダイナミック閾値で判定。閾値はレンジ幅に応じてスケーリング（5%→0.35、10%→0.40、15%+→0.45）。レンジ幅8%以上ではHH/HL判定と矛盾してもオーバーライド
* タイムフレーム引数対応（ルックバック期間の動的調整）

### デリバティブ分析拡張

* **OI残存率分析**: 価格変動に対するOI変化率の比率を算出。急落後にOIが残存している場合は `positions_remaining` + `volatilityBias: high` と判定し、再度の急変動リスクを警告

### 群集心理分析 (`crowd-psychology.ts`)

Funding / Premium / OI / Fear&Greed の複合条件で以下のパターンを検出:
* **ショートスクイーズリスク**: Funding負 + Premium負 + OI維持/増加
* **ロングスクイーズリスク**: Funding正 + Premium正 + OI維持/増加
* **FOMO買い**: Fear&Greed 75+ + OI急増 + 新規ロング
* **パニック売り**: Fear&Greed 25- + OI急増 + 新規ショート

各パターンに `biasAdjustment` 値を持ち、シグナルスコアに反映。

### 価格丸め (`roundToTradingPrice`)

最終出力の価格（エントリー/損切/利確）を実用的な取引単位に丸める:
* BTC ($10,000+): $10単位
* ETH ($1,000+): $5単位
* SOL ($100+): $1単位
* 中位アルト ($10+): $0.1単位
* 小型アルト: $0.01単位

丸め方向はユーザーに有利な側に統一:
* エントリー: ロング=切り下げ、ショート=切り上げ
* 損切: ロング=切り下げ、ショート=切り上げ（SL幅を広めに）
* 利確: ロング=切り上げ、ショート=切り下げ（利幅を広めに）

PR比は丸め後の値で再計算。

### 信頼度スコア (`calcConfidence`)

0〜100のスコアに以下の要因を反映:
* タイムフレーム一致度
* トレンド強度
* RR比
* ダイバージェンス
* デリバティブ確認
* トップトレーダー比率
* VRVP方向一致
* 階層分析一致
* 経済指標リスク
* **不足データの明示**: 未取得のタイムフレーム、OI履歴なし、Taker売買比率なし、大口取引データなし等を `missingData` 配列で報告

### VRVP描画 (`VrvpPrimitive.ts`)

* 買い出来高(シアン) + 売り出来高(ピンク) の横並びスタック描画
* 買い/売り推定: `buyRatio = (close - low) / (high - low)` (価格アクションヒューリスティック)
* POC: 黄色ボーダー、VA内外で透明度差

### UIコンポーネント

| コンポーネント | 概要 |
| --- | --- |
| `DashboardView` | 統合ダッシュボード（トレードスタイル選択タブ付き） |
| `Conclusion` | シグナル結論 + 分析ブレークダウン + 不足データ表示 |
| `HyperliquidChart` | TradingView風チャート + ボリュームプロファイル |
| `PRComparison` | ロング vs ショート比較 (指値ベース、丸め済み価格) |
| `PositionSimulator` | ポジション入力シミュレーション |
| `PositionManager` | ポジション管理・P&L追跡 |
| `NewsSection` | ニュース表示 + 関連度/インパクト |
| `MarketSummary` | 現在価格、24h変動、出来高、OI |

### APIルート

| ルート | 用途 |
| --- | --- |
| `/api/analyze` | メインシグナル生成（tradingStyle パラメータ対応） |
| `/api/klines` | Binanceローソク足 |
| `/api/klines-hl` | Hyperliquidローソク足 |
| `/api/news` | ニュース取得 |
| `/api/economic-calendar` | 経済イベント |
| `/api/market-data` | 統合マーケットデータ |
| `/api/refresh-external` | キャッシュ更新 (cron, 30分間隔) |
| `/api/price-alert` | 価格急変アラート (cron, 3分間隔、閾値超過でTelegram通知) |

## 設計原則

* **無料API優先**: 全コア機能は認証不要の無料APIで動作
* **積極的キャッシュ**: ニュース/Fear&Greed/経済カレンダーはインメモリキャッシュ
* **グレースフルフォールバック**: Hyperliquid → Binance のフォールバック
* **シミュレーション限定**: 実取引連携なし (安全性優先)
* **日本語UI**: 全ラベル・説明は日本語
* **指値引きつけ**: エントリーは現在値から十分に離れた強いS/Rレベルに設定（成行エントリーを回避）
* **構造ベースSL**: S/Rレベルを根拠とした損切設定、ATRによるガードレール、絶対最低幅保証
* **階層フィルター**: 上位足バイアスに逆行するエントリーを自動抑制
* **実用的な価格表示**: 取引単位に丸めた価格、ユーザー有利方向の丸め

## 環境変数 (オプション)

```
TELEGRAM_BOT_TOKEN=     # Telegram通知
TELEGRAM_CHAT_ID=       # 通知先チャット
```

コア機能は環境変数なしで動作する。

## 改修履歴

### v1 (改修1〜3)
* 改修1: 階層分析のハードフィルター化（上位足逆行エントリーをwaitに降格）
* 改修2: CLAUDE.md のSL記載を実コードに合わせて更新
* 改修3: `crowd-psychology.ts` 新規作成（群集心理パターン検出）

### v2 (改修4〜7)
* 改修4: トレードスタイル別タイムフレームプリセット対応（スキャルピング/デイトレード/スイング）
* 改修5: S/R設定への心理的節目ボーナス追加
* 改修6: OI残存率によるボラティリティ予測指標
* 改修7: 信頼度コンポーネントに不足データの明示

### v3 (改修8〜10)
* 改修8: エントリーを「十分に引きつけた強いS/R」に設定（findEntrySupport/Resistance新設）
* 改修9: blendedAtrの短期足バイアス修正（セットアップ足ベース）+ SL絶対最低幅0.7%
* 改修10: analyzeTrend()のトレンド判定精度改善（レンジ幅ベース補助判定、TF別ルックバック）

### v3.1 (改修11)
* 改修11: トレード価格の丸め処理（$10単位丸め、ユーザー有利方向、丸め後PR再計算）

### v3.2 (改修12)
* 改修12: ポジションにトレードスタイルタグ追加（スキャ/デイトレ/スイングのバッジ表示）+ 保有目安時間調整（スキャルピング1h、デイトレ8h、スイング3d）

### v4 (改修13〜15)
* 改修13(11A/B): analyzeTrend()閾値再調整 — レンジポジション閾値0.25→0.35緩和、rangeBasedBiasがレンジ幅8%以上でHH/HL判定をオーバーライド
* 改修14(11C): スイングハイ/ロー検出に最小振幅フィルタ追加（4H: 2%未満の動きをスイングと見なさない）
* 改修15(12): 階層フィルターを段階的降格に変更（一律wait→信頼度減衰方式、カウンタートレンドはPR2.5以上で許容+警告表示）
* 改修16(13): シグナル集計に環境認識バイアス乗数を導入（逆行シグナル×0.5減衰、順行シグナル×1.3ブースト）

### v4.1 (改修17〜18)
* 改修17: レンジ幅ベース補助判定のルックバック期間を独立設定化（`RANGE_LOOKBACK`: 4H=80本/約13日）。`SWING_LOOKBACK*3`では78K→67.8Kのような1週間超のトレンドを捕捉できなかった問題を修正
* 改修18: レンジポジション閾値をダイナミック化。レンジ幅に応じて閾値をスケーリング（5%→0.35、10%→0.40、15%+→0.45）。狭いレンジでは端にいる時のみ、広いレンジでは中間寄りでも方向性を判定

### v5 (改修19)
* 改修19A: RANGE_LOOKBACK 4H足を60→80本（約13日分）に拡大。78Kの高値がウィンドウ境界で外れる問題を修正
* 改修19B: detectSwingsのスコープをRANGE_LOOKBACK本数に制限。全量キャンドルから古い上昇構造を拾いhhhlDirection=uptrendになる問題を修正

### v5.1 (改修20)
* 改修20: 価格急変アラートAPI (`/api/price-alert`) 新設。軽量エンドポイントで3分間隔Cron対応、シンボルごとの変動閾値（BTC=2%, ETH=3%, その他=5%）超過時にTelegram通知
